<<<<<<< HEAD
# SubstrateMiner Documentation

Welcome to the SubstrateMiner documentation. This page provides an overview of the tool, installation instructions, usage examples, and more.

## Overview

SubstrateMiner is a tool for performing multiple sequence alignments using various methods such as ClustalOmega, MAFFT, and MUSCLE. It is designed to be easy to use and integrate into your bioinformatics workflows.

## Installation

To install SubstrateMiner, follow these steps:

1. Clone the repository:
    ```sh
    git clone https://github.com/yourusername/substrateminer.git
    cd substrateminer
    ```

2. Create and activate a conda environment:
    ```sh
    conda env create -f environment.yml
    conda activate substrate-miner
    ```

3. Install the package:
    ```sh
    pip install -e .
    ```

## Usage

To perform multiple sequence alignment, use the following command:

```sh
substrateminer msa -i <input_file> -o <output_file> -m <method>
```

- `<input_file>`: Path to the input file containing sequences in FASTA format.
- `<output_file>`: Path to the output file where the alignment results will be saved.
- `<method>`: Alignment method to use. Options are `clustalomega`, `mafft`, or `muscle`.

### Example

```sh
substrateminer msa -i sequences.fasta -o alignment.aln -m clustalomega
```

## Running Tests

To run the unit tests, use the following command:

```sh
conda run -n substrate-miner python -m unittest discover -s unittests -p "test_*.py"
```

## GitHub Actions CI

The project includes a GitHub Actions workflow for continuous integration. The workflow is defined in `.github/workflows/python-ci.yml` and performs the following steps:

- Checks out the repository.
- Sets up Python.
- Caches the conda environment.
- Installs Miniconda and creates the conda environment.
- Runs CLI tests with pip.
- Runs unit tests with unittest.

## License

This project is licensed under the MIT License. See the [LICENSE](LICENSE) file for details.

## Contributing

We welcome contributions to SubstrateMiner. If you would like to contribute, please fork the repository and submit a pull request. For major changes, please open an issue first to discuss what you would like to change.

## Contact

If you have any questions or need further assistance, please contact us at [your-email@example.com](mailto:your-email@example.com).
````

### /Users/robqiao/Downloads/gits/substrateminer/docs/_config.yml

Create a configuration file for GitHub Pages.

<file>
```yaml

title: SubstrateMiner Documentation
description: Documentation for the SubstrateMiner tool
theme: jekyll-theme-cayman
=======
# This file is used to configure the Jekyll site.

theme: jekyll-theme-minimal
title: SubstrateMiner
description: A tool for mining substrate sequences from protein structures.
show_downloads: true
>>>>>>> 099ae590628b4c76e884be8545c903c0abd6580d
